- defaults:
    link:
      create: true
      relink: true
      force: true
- clean: ["~", "~/.config"]
- link:
    ~/.config/fontconfig:
    ~/.config/nvim:
    ~/.config/clangd:
    ~/.config/yazi:
    ~/.config/fish:
    ~/.local/share/applications:
    ~/.gitconfig:
    ~/.latexmkrc:
    ~/.bashrc:
    ~/.bash_profile:
    ~/bin:

- create:
    - ~/.local/bin

- shell:
    - [git submodule update --init --recursive, Installing submodules]
